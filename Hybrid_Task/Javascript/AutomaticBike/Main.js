const {turnOff,turnOn,changeGear,accelerate,decelerate} = require("./Functions");
const prompt = require('prompt-sync')();

let menu = `
            1. Start Car
            2. Stop Car
            3. Accelerate
            4. Decelerate  
            5. leave bike    
        `;